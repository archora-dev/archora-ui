{
  "name": "@archora/docs",
  "version": "0.0.0",
  "private": true,
  "description": "VitePress documentation for Archora UI.",
  "type": "module",
  "scripts": {
    "dev": "vitepress dev .",
    "build": "vitepress build .",
    "typecheck": "vue-tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@archora/ui": "workspace:*"
  }
}
